{
  "name": "bound-native-methods",
  "version": "0.1.6",
  "description": "Bound native virtual methods to be used together with ES7 :: bind operator.",
  "author": "G. Kay Lee <balancetraveller+github@gmail.com>",
  "scripts": {
    "setup": "npm cache clean && npm install",
    "update": "rm -rf node_modules && npm-check-updates -u && npm run setup",
    "build": "gulp build",
    "deploy": "npm run build && ./deploy"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gsklee/bound-native-methods"
  },
  "keywords": [
    "virtual method",
    "method extraction",
    "function bind",
    "bind operator",
    "bind",
    "es6",
    "es7",
    "babel"
  ],
  "dependencies": {},
  "devDependencies": {
    "babel": "^5.5.3",
    "gulp": "^3.9.0",
    "gulp-load-plugins": "^0.10.0",
    "gulp-babel": "^5.1.0"
  },
  "engines": {
    "iojs": ">=2.0.0"
  },
  "license": "MIT"
}
